/** Pure host half for the browser-discovered self-update UI plugin. */

/** Host plugin body; the Update button lives in the browser export and Host Remote domain. */
export function apply(): void {}
